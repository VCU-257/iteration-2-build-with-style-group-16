# Student Name: [Mark]

## 1. My Assigned Work
*I was responsible for implementing the scheduling page (`schedulePage.html`). This included building the fulfillment toggle (delivery vs. in-store pickup), a date picker with time window slots, a delivery address form, and a pickup info panel. I also wired the order summary to read live cart data from `localStorage` (matching the format used by the cart page) and added form validation and a confirmation banner on submit.

## 2. AI / LLM Usage
*Yes, I used AI tools during this process.*
* **I used it to analyze my teammates' existing HTML files and extract the shared design system** *(e.g., colors, fonts, navbar structure) so my page would be consistent without modifying their code.*
* **It helped me implement the localStorage integration for the order summary, reading the cart data in the same format Ian's cart page saves it. I reviewed the `JSON.parse` / `localStorage.getItem` logic to make sure I understood how the data flows between pages.**

## 3. Live Site Link
* **Live URL:** [https://vcu-257.github.io/iteration-2-build-with-style-group-16/schedulePage.html]
